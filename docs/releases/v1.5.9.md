# arg-bridge v1.5.9

增加安全的托管释放操作。

- `/tmux release` 解除 Bridge 对 managed tmux session 的标记并停止 Bridge 观察句柄，但保留 session、pane 和 Codex/Claude 进程；不会发送 Ctrl-C 或删除 pane。
- `/tmux unbind` 继续只处理 external 绑定；对 managed session 会提示使用 `/tmux release`。
- 释放后 tmux pane 可作为 external pane 重新绑定，或由用户手动运行新的 agent。

